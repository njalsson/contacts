import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        padding: 20,
        justifyContent: 'flex-start',
    },
    addButton: {
        position: 'absolute',
        elevation: 2,
        bottom: 50,
    }
});
