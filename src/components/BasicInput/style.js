import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    input: {
        width: '100%',
        fontSize: 20,
        borderBottomWidth: 1,
        borderColor: '#6C757D',
        marginBottom: 15,
        paddingBottom:15,
    },
    focused: {
        borderColor: 'blue',
    }
})