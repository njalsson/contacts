import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    List: {
        padding: 5,
        marginTop: 10,
    },
    listHeader: {
        color: 'rgba(0, 0, 0, 0.6)',
        fontSize: 20,
        marginBottom: 5,
    },

});
