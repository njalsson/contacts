import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    List: {
        backgroundColor: 'red',
        padding: 5,
        marginTop: 10,
        color: 'green',
    },

});
